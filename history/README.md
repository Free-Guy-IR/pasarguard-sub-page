# Older versions of the page

These three files are versions of `index.html` that were live on the panel before
this repository started tracking every change. They existed only as timestamped
backups on the server, and the server now keeps a single rollback copy, so they are
kept here instead of being lost.

| file | sha256 (first 16) | what it was |
|---|---|---|
| `2026-09-07-before-l2tp-work.html` | `b244b3be63b449f5` | the page as it stood on 7 September, before the L2TP section |
| `2026-09-07-pre-l2tp.html` | `1de14fa2cab22c66` | the copy taken immediately before the L2TP section was added |
| `2026-09-08-pre-single-device-notice.html` | `a9516e0b3289321f` | the copy taken before the single-device notice was added |

Every other version the server was holding is already reachable through this
repository's history of `index.html`, and the 292 KB page that preceded the redesign
is still on the server at `/root/new_sub.html`.

Nothing here is served. They are kept for reference and for rollback further back
than one step.
